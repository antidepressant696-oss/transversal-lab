# Transversal Lab Unified v1.0 navigation fix

Теперь все страницы связаны в единую структуру:

Единая лаборатория (`index.html`)
→ Lab 1 / Lab 2 / Lab 3 / Lab 4 / Lab 5
→ Мой профиль (`profile.html`)
→ Кабинет преподавателя (`teacher.html`)

На каждой странице Labs добавлено глобальное меню с переходами обратно в единую лабораторию, профиль и кабинет.

Таким образом, Lab 1 теперь является **модулем**, а не отдельным вторым сайтом. Его отдельный дизайн сохранён.
